<adonisControls:AdonisWindow x:Class="Yor.Views.Loading"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:local="clr-namespace:Yor"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
                             IconVisibility="Collapsed" Width="500" Height="300" Background="#21252B">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}" >
            <Setter Property="Title" Value="Loading"/>
        </Style>
    </adonisControls:AdonisWindow.Style>
    <Grid>
        <TextBlock x:Name="Log" Text="Loading" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,-50,0,0" Foreground="White"/>
        <ProgressBar x:Name="Progress" Value="0" Height="20" Margin="20,0,20,0"/>
    </Grid>
</adonisControls:AdonisWindow>